# Optional: Add an Anthropic API key to generate components
# If you don't, static code will be generated instead.
ANTHROPIC_API_KEY=

# JWT Secret for session management
# CRITICAL: Use a strong, random secret in production
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Database URL (automatically configured for development)
DATABASE_URL="file:./dev.db"